name: news_app_using_riverpod
description: A new Flutter project.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^5.0.0
  flutter_riverpod: ^1.0.3+1
  freezed_annotation: ^2.1.0
  cached_network_image: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: any
  freezed: ^2.0.4

flutter:

  uses-material-design: true

